[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Keep CI focused on correctness errors. Style modernization can be adopted
# incrementally without making unrelated pull requests fix the entire backend.
select = ["E4", "E7", "E9", "F"]
